version: 2.1

jobs:
  hello-job:
    machine: true
    resource_class: facebook/rocksdb-benchmark-sys1
    steps:
      - run: echo "hello world"
      - run: hostname

workflows:
  my-workflow:
    jobs:
      - hello-job
