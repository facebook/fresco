<?xml version="1.0" encoding="utf-8"?>
<ScrollView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MediaControlFragment"
    >
  <LinearLayout
      android:layout_width="match_parent"
      android:layout_height="wrap_content"
      android:paddingBottom="@dimen/activity_vertical_margin"
      android:paddingLeft="@dimen/activity_horizontal_margin"
      android:paddingRight="@dimen/activity_horizontal_margin"
      android:paddingTop="@dimen/activity_vertical_margin"
      android:orientation="vertical"
      >
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingTop="@dimen/activity_vertical_margin"
        android:orientation="horizontal"
        >
      <!-- The main animation -->
      <ImageView
          android:id="@+id/animation_container"
          android:layout_width="@dimen/animation_width"
          android:layout_height="@dimen/animation_height"
          />

      <!-- Animation controls -->
      <include android:id="@+id/controls" layout="@layout/controls"/>
    </LinearLayout>

    <!-- Backend selector -->
    <include layout="@layout/backends"/>
  </LinearLayout>
</ScrollView>
